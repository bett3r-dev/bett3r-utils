# Tell npm every @bett3r-dev/* package lives on GitHub
@bett3r-dev:registry=https://npm.pkg.github.com

# Always use auth on that registry

# Inject your PAT at runtime – never commit the raw token
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}